apiVersion: v1
kind: Namespace
metadata:
  name: NAMESPACE


---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-pod
  namespace: NAMESPACE
  labels:
    app: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
      - name: api-container
        image: ACCT.dkr.ecr.REG.amazonaws.com/REPO:VER
        ports:
        - containerPort: 80
