apiVersion: v1
kind: Namespace
metadata:
  name: NAMESPACE


---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  namespace: NAMESPACE
  labels:
    app: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
      - name: api
        image: ACCT.dkr.ecr.REG.amazonaws.com/apiapp:VER
        ports:
        - containerPort: 80
